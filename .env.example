# Server Configuration
PORT=3000
NODE_ENV=development

# Token Generator Sources (comma-separated for multiple sources)
TOKEN_SOURCE_PRIMARY=https://dramabox-token.vercel.app/token
# TOKEN_SOURCE_BACKUP=https://your-backup-token-source.com/token

# Optional: Custom configuration
# CACHE_TTL=82800  # Token cache TTL in seconds (default: 23 hours)
